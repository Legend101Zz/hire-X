[project]
name = "backend-v2"
version = "2.0.0"
description = "AI-powered candidate search with smart matching and instant results"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pypdf2>=3.0.1",
]

